import React from "react";
import Categories from "../components/Categories";

const MainContainer = () => {
  return (
    <div className="cat">
      <Categories />
    </div>
  );
};

export default MainContainer;
